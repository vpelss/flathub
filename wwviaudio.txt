
--- wwviaudio.c	2026-08-13 12:15:36.462208316 -0400
+++ wwviaudio2.c	2026-08-14 15:27:21.071056345 -0400
@@ -143,29 +143,6 @@
 static struct audio_queue_entry *audio_chain_tail[WWVIAUDIO_CHAIN_COUNT] = { 0 };
 
 
-static size_t strlcpy(char *dest, const char *src, size_t n)
-{
-	size_t i;
-
-	if (n == 0)
-		return 0;
-
-	for (i = 0;; i++) {
-		if (i < n)
-			dest[i] = src[i];
-		else {
-			dest[n - 1] = '\0';
-			break;
-		}
-		if (src[i] == '\0')
-			return i;
-	}
-	for (;; i++)
-		if (src[i] == '\0')
-			break;
-	return i;
-}
-
 int wwviaudio_read_ogg_clip_into_allocated_buffer(char *filename, int16_t **sample, int *nsamples)
 {
 	uint64_t nframes;
