commit 67aae9f2afe6c2cfc0b84c132ac321b955ec6845
Author: Stephen M. Cameron <stephenmcameron@gmail.com>
Date:   Wed Aug 12 13:34:38 2026 -0400

    Do not implement home made strlcpy()
    
    Signed-off-by: Stephen M. Cameron <stephenmcameron@gmail.com>

diff --git a/Makefile b/Makefile
index 502dffa..1f7a884 100644
--- a/Makefile
+++ b/Makefile
@@ -9,7 +9,7 @@ SNDFLAGS:=$(shell $(PKG_CONFIG) --cflags portaudio-2.0)
 
 SNDLIBS:=$(shell $(PKG_CONFIG) --libs portaudio-2.0 vorbisfile)
 
-LIBS=${SDLLIBS} -lpng -lm ${SNDLIBS}
+LIBS=${SDLLIBS} -lpng -lm -lbsd ${SNDLIBS}
 
 CFLAGS=-fsanitize=address -Wall -Wextra --pedantic ${SDLCFLAGS} -g3 -pthread ${SNDFLAGS}
 LDFLAGS=-fsanitize=address
diff --git a/wwviaudio.c b/wwviaudio.c
index 8525b10..4455c69 100644
--- a/wwviaudio.c
+++ b/wwviaudio.c
@@ -28,6 +28,7 @@
 #include <sys/stat.h>
 #include <unistd.h>
 #include <string.h>
+#include <bsd/string.h>
 #include <stdlib.h>
 #include <pthread.h>
 #include <math.h>
@@ -142,30 +143,6 @@ static struct audio_queue_entry *audio_queue = NULL;
 static struct audio_queue_entry *audio_chain_head[WWVIAUDIO_CHAIN_COUNT] = { 0 };
 static struct audio_queue_entry *audio_chain_tail[WWVIAUDIO_CHAIN_COUNT] = { 0 };
 
-
-static size_t strlcpy(char *dest, const char *src, size_t n)
-{
-	size_t i;
-
-	if (n == 0)
-		return 0;
-
-	for (i = 0;; i++) {
-		if (i < n)
-			dest[i] = src[i];
-		else {
-			dest[n - 1] = '\0';
-			break;
-		}
-		if (src[i] == '\0')
-			return i;
-	}
-	for (;; i++)
-		if (src[i] == '\0')
-			break;
-	return i;
-}
-
 int wwviaudio_read_ogg_clip_into_allocated_buffer(char *filename, int16_t **sample, int *nsamples)
 {
 	uint64_t nframes;
